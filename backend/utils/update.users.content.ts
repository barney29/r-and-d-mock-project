/**
 * USAGE: if the post is modified and the content type changed
 * @params usersWithThePost: users associated with the post
 * @params id: the post id
 *
 *
 */

// import { Schema } from "mongoose";
// import User from "../models/user.model";

// export const updateUsersContent = async (usersWithThePost: typeof User, id: Schema.Types.ObjectId) => {
//   try {
//     for (let user of usersWithThePost) {
//     }
//   } catch (e) {

//   }
// }
